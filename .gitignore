# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
package-lock.json

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*.local

# Claude Code agent harness
.claude/worktrees/
.claude/settings.local.json
# Local, machine-specific MCP server config (secrets / cred paths). Commit .mcp.json.example instead.
.mcp.json
tasks/todo.md
tasks/lessons.md
tasks/specs/*.state.json
qa-env.json
graphify-out/

# One-time Trello->Jira migration tooling (kept local; the record lives in Jira labels + the id-map)
/scripts/migrate-trello-to-jira.mjs
/scripts/trello-export.json
/scripts/trello-jira-id-map.json
/scripts/dry-run-plan.txt

# obsidian
.obsidian/

# Sermon PDF generator — generated output (never committed; use --out $TMPDIR)
.claude/scripts/predica/**/*.pdf
.claude/scripts/predica/**/*.png

# /predica harness artifacts — audio, transcripts, sermon.json, generated PDFs (large, not source)
tasks/predicas/

# Contentful cold-backup exports (heavy-cutover insurance; local only)
apps/web/scripts/contentful/backups/

# Playwright MCP — local browser console logs (temporary test artifacts)
.playwright-mcp/

# QA evidence — per-ticket browser screenshots (binary, large; uploaded to Jira, not source of truth)
# Agents now write to tasks/qa-evidence/<ticket>/<run>/; the root-anchored globs are a safety net
# for any stray bare-filename screenshot a tool drops at the repo root.
tasks/qa-evidence/
tasks/qa-runs/
/ac*.png
/qa-*.png

# turborepo cache
.turbo/

# monorepo (apps/*, packages/*) — de-anchored build/dep/cache dirs
**/node_modules/
**/.next/
**/out/
**/build/
**/coverage/
**/test-results/
**/playwright-report/
**/blob-report/
**/.playwright/
